server:
  port: 9093
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    name: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: admin
    password: root
    driverClassName: org.postgresql.Driver
  application:
    name: user-service
jwt:
  secret: reactjava@123
eureka:
  instance:
    hostname: localhost


